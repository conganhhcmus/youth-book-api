export const enum ComicStatusType {
    updating = 0,
    completed = 1,
}

export const ComicStatus = {
    updating: 0,
    completed: 1,
};

export const enum TopComicType {
    all = 'all',
    daily = 'daily',
    weekly = 'weekly',
    monthly = 'monthly',
}
